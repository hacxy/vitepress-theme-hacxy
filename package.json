{
  "name": "hacxy-blog-root",
  "version": "0.0.0",
  "private": true,
  "author": "Hacxy",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "theme:dev": "pnpm -F vitepress-theme-hacxy run dev",
    "theme:build": "pnpm -F vitepress-theme-hacxy run build",
    "blog:dev": "pnpm -F blog run dev",
    "blog:build": "pnpm -F blog run build",
    "blog:preview": "pnpm -F blog run preview"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "typed.js": "^2.1.0"
  }
}
